//primeiro

/*No pior caso, ele percorre todo o array para cada elemento, resultando em uma complexidade de tempo de O(n^2). No melhor caso, se o array já estiver ordenado, ainda assim ele terá que fazer todas as comparações, resultando em O(n^2). No caso médio, também é O(n^2) devido ao número máximo de comparações e trocas.*/

//segundo

/*No pior caso, se o array estiver em ordem decrescente, ainda precisará fazer todas as comparações e trocas, resultando em O(n^2). No melhor caso, se o array já estiver ordenado, a função só precisará percorrê-lo uma vez, resultando em O(n). No caso médio, mesmo com a otimização, ainda terá a complexidade de pior caso de O(n^2) para uma gama variada de entradas.*/

//AlgumaCoisa

/*No pior caso, o loop mais interno executa um número variável de operações, resultando em O(n^3). No melhor caso, se n for pequeno, poderíamos considerar O(1) para o número de operações realizadas, mas, em geral, também é O(n^3) para entradas maiores. No caso médio, a complexidade é semelhante ao pior caso, resultando em O(n^3) devido ao número máximo de operações realizadas pelos loops.*/

//AlgumaCoisa2

/*No pior caso, tem uma complexidade de O(n^3) similar à função anterior, pois o número máximo de operações realizadas pelos loops ainda é o mesmo. No melhor caso, pode-se considerar O(1) para um número pequeno de operações, mas para entradas maiores também é O(n^3). No caso médio, a complexidade também é O(n^3) devido ao número máximo de operações realizadas pelos loops.*/
